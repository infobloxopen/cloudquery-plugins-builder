apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudquery-plugin-s3
  labels:
    app: cloudquery-plugin-s3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudquery-plugin-s3
  template:
    metadata:
      labels:
        app: cloudquery-plugin-s3
    spec:
      containers:
        - name: plugin
          image: ghcr.io/infobloxopen/cloudquery-plugin-s3:v7.10.2
          ports:
            - containerPort: 7777
              protocol: TCP
          readinessProbe:
            tcpSocket:
              port: 7777
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 7777
            initialDelaySeconds: 10
            periodSeconds: 30
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: cloudquery-plugin-s3
spec:
  selector:
    app: cloudquery-plugin-s3
  ports:
    - port: 7777
      targetPort: 7777
      protocol: TCP
